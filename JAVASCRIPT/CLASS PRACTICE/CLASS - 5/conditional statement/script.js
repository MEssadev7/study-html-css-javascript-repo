// if ,else , else if,

// reserve words we can't use it as a variable

// alternative of if else
// let result = age >= 18 ? "your eligible" : "your not eligiable";

let age = Number(prompt("enter your age", 18));
if (age >= 18) {
  document.write("your eligiable");
} else {
  document.write("your not eligiable");
}
